apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vote
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: vote
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: vote-service
      path: /metrics
      interval: 15s
