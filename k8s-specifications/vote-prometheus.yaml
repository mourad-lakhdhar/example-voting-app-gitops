apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vote
  namespace: example-voting-app
spec:
  selector:
    matchLabels:
      app: vote
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: 8080
      path: /metrics
      interval: 15s
